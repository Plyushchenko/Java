import MD5.MD5MultiThread;
import MD5.MD5SingleThread;
import org.junit.Test;

import java.io.IOException;
import java.nio.file.Paths;
import java.security.NoSuchAlgorithmException;
import java.util.Arrays;

public class MD5Test {
    @Test
    public void simpleTest() throws IOException, NoSuchAlgorithmException {
/*        new MD5SingleThread().getHash(Paths.get("../input.txt"));
        System.out.println(Arrays.toString(new MD5SingleThread().getHash(Paths.get("../cdl"))));
        System.out.println(Arrays.toString(new MD5MultiThread().getHash(Paths.get("../cdl"))));
        System.out.println(Paths.get("../../JavaPractice/cdl").getFileName());
*/
    }
}
